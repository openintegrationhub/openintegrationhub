/**
 * Abstraction layer for accessing flows.
 */
class FlowsDao {
    /**
     * Find a flow by ID.
     * @param {*} id
     */
    findById(id) {
        throw new Error('To be implemented');
    }
}

module.exports = FlowsDao;
