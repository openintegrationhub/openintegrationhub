'use strict';

const ERROR = 'error';
const ALWAYS = 'always';
const NEVER = 'never';

module.exports = {
    env: {
        es6: true,
        node: true,
        mocha: true
    },
    parserOptions: {
        ecmaVersion: 2018
    }
};
