apiVersion: v1
kind: Service
metadata:
  labels:
    app: integration-content-repository
  name: integration-content-repository
  namespace: oih-dev-ns
spec:
  type: NodePort
  selector:
      app: integration-content-repository
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
